{% extends 'base.html.twig' %}


{% block body %}
    {{ dump() }}
    <br>
    <h1>CREATION DE VOTRE COMMANDE ETAPE 2</h1>
        <h2>Veuillez remplir les informations ci-après :</h2>

            {{  form_start(formBillet) }}
                {{ form_errors(formBillet) }}
                {{ form_widget(formBillet) }}
                <button type="submit" class="btn btn-default" >Etape suivante</button>
            {{ form_end(formBillet) }}

         <br>

{% endblock %}

{% block javascripts %}
{% endblock %}
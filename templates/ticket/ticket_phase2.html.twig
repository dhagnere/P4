{% extends 'base.html.twig' %}


{% block body %}
<body>
        {% for msg in app.session.flashbag.get('success') %}
            <div class="alert alert-success">
                {{ msg }}
            </div>
        {% endfor %}
        <div class="fa-2x">
            <div class="titre-accueil">
            <h2>CREATION DE VOTRE COMMANDE ETAPE 2</h2>
            </div>
                <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner">
                        <div class="carousel-item active">
                            <h3>Veuillez remplir les informations ci-après :</h3>

                                {{  form_start(formBillet) }}
                                {{ form_errors(formBillet) }}
                                {{ form_widget(formBillet) }}
                                <button type="submit" class="button-access-billet2" >Etape suivante</button>
                                {{ form_end(formBillet) }}


                        </div>
                    </div>
                </div>
            <br>
        </div>
</body>
{% endblock %}
